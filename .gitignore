/target
/tmp
stryke.lock
staging/
*.tar.gz
*.tar.gz.sha256

# Override the global `docs/` ignore — docs/ here is the project's
# GitHub Pages content (index.html, report.html, shared CSS/JS).
!docs/
!docs/*

# Override the global `bin/` ignore — bin/ here is the project's CLI
# entrypoint (stryke scripts, NOT compiled binaries).
!bin/
!bin/*
